# Normalize all text files to LF in the repo, checkout as-is on all platforms
* text=auto eol=lf

# Scripts and ES modules — must stay LF to avoid shebang/parse issues on Windows
*.sh text eol=lf
*.mjs text eol=lf
