generator: nap-docgen
version: "0.8.9"
commands:
  - name: "add"
    full_path: "add"
    about: "Add a file representation to an entity manifest"
    usage: "Usage: add [OPTIONS] --format <FORMAT> <URI> <KEY> <FILE>"
    hidden: false
    arguments:
      - name: "uri"
        about: "NAP URI"
        required: true
      - name: "key"
        about: "Representation key. e.g., \"reference_image\""
        required: true
      - name: "file"
        about: "File path to the asset"
        required: true
    options:
      - name: "format"
        about: "Asset format. e.g., \"png\", \"glb\""
      - name: "message"
        about: "Commit message"
        default: "add representation"
      - name: "author"
        about: "Author identifier"
        default: "nap-cli"
    flags:
      - name: "help"
        about: "Print help"
  - name: "auth"
    full_path: "auth"
    about: "Manage secure Portals Cloud authentication"
    usage: "Usage: auth <COMMAND>"
    hidden: false
    flags:
      - name: "help"
        about: "Print help"
    subcommands:
      - name: "login"
        full_path: "auth login"
        about: "Sign in through the configured Lore authentication service"
        usage: "Usage: login [OPTIONS]"
        hidden: false
        options:
          - name: "api_key_env"
            about: "Environment variable containing the API key"
            default: "PORTALS_CLOUD_API_KEY"
        flags:
          - name: "api_key"
            about: "Exchange a service-account API key instead of opening a browser"
          - name: "no_browser"
            about: "Print the login URL without opening a browser"
          - name: "help"
            about: "Print help"
      - name: "logout"
        full_path: "auth logout"
        about: "Remove locally cached Lore credentials"
        usage: "Usage: logout"
        hidden: false
        flags:
          - name: "help"
            about: "Print help"
      - name: "status"
        full_path: "auth status"
        about: "Show the currently cached Lore identity without printing tokens"
        usage: "Usage: status"
        hidden: false
        flags:
          - name: "help"
            about: "Print help"
  - name: "backend"
    full_path: "backend"
    about: "Configure or inspect the version-control backend"
    usage: "Usage: backend <COMMAND>"
    hidden: false
    flags:
      - name: "help"
        about: "Print help"
    subcommands:
      - name: "configure"
        full_path: "backend configure"
        about: "Configure the version-control backend"
        long_about: |
          Configure the version-control backend.

          After configuration, existing unversioned repositories in this NAP home are offered an initial commit so their current filesystem state becomes the repository baseline (unless --no-initial-commit is given).
        usage: "Usage: configure [OPTIONS] <BACKEND>"
        hidden: false
        arguments:
          - name: "backend"
            about: "Backend type: local or remote"
            required: true
        options:
          - name: "endpoint"
            about: "Remote endpoint URL (required for remote backend)"
          - name: "workspace_id"
            about: "Workspace ID (for remote backend)"
        flags:
          - name: "initial_commit"
            about: "Bootstrap existing repositories with an initial commit without prompting"
          - name: "no_initial_commit"
            about: "Skip bootstrapping existing repositories with an initial commit"
          - name: "help"
            about: "Print help (see more with '--help')"
      - name: "status"
        full_path: "backend status"
        about: "Show the current version-control backend configuration"
        usage: "Usage: status"
        hidden: false
        flags:
          - name: "help"
            about: "Print help"
  - name: "branch"
    full_path: "branch"
    about: "Create or list branches"
    usage: "Usage: branch <REPOSITORY> [NAME]"
    hidden: false
    arguments:
      - name: "repository"
        about: "Repository name"
        required: true
      - name: "name"
        about: "Branch name to create. Omit to list all branches"
        required: false
    flags:
      - name: "help"
        about: "Print help"
  - name: "choose"
    full_path: "choose"
    about: "Choose backend provider"
    usage: "Usage: choose <COMMAND>"
    hidden: false
    flags:
      - name: "help"
        about: "Print help"
    subcommands:
      - name: "backend"
        full_path: "choose backend"
        about: "Choose backend provider"
        usage: "Usage: backend [OPTIONS] <PROVIDER>"
        hidden: false
        arguments:
          - name: "provider"
            about: "Provider type: local, portals-cloud, or remote"
            required: true
        options:
          - name: "remote_url"
            about: "Remote URL (required for remote provider)"
          - name: "workspace_id"
            about: "Workspace ID (for remote provider)"
        flags:
          - name: "reset"
            about: "Reset the provider configuration file"
          - name: "help"
            about: "Print help"
  - name: "commit"
    full_path: "commit"
    about: "Commit changes to a repository repository"
    usage: "Usage: commit [OPTIONS] --message <MESSAGE> <REPOSITORY>"
    hidden: false
    arguments:
      - name: "repository"
        about: "Repository name"
        required: true
    options:
      - name: "message"
        about: "Commit message"
      - name: "author"
        about: "Author identifier"
        default: "nap-cli"
    flags:
      - name: "help"
        about: "Print help"
  - name: "content-hash"
    full_path: "content-hash"
    about: "Compute the BLAKE3 content hash of a file"
    usage: "Usage: content-hash <FILE>"
    hidden: false
    arguments:
      - name: "file"
        about: "Path to the file to hash"
        required: true
    flags:
      - name: "help"
        about: "Print help"
  - name: "create"
    full_path: "create"
    about: "Create a new entity manifest"
    usage: "Usage: create [OPTIONS] --repository <REPOSITORY> --name <NAME> <ENTITY_TYPE> <ENTITY_ID>"
    hidden: false
    arguments:
      - name: "entity_type"
        about: "Entity type (any non-empty string, e.g. character, location, custom-type)"
        required: true
      - name: "entity_id"
        about: "Entity ID (slug). e.g., \"woody\""
        required: true
    options:
      - name: "repository"
        about: "Repository name"
      - name: "name"
        about: "Human-readable name"
      - name: "author"
        about: "Author identifier"
        default: "nap-cli"
    flags:
      - name: "help"
        about: "Print help"
  - name: "diff"
    full_path: "diff"
    about: "Show diff between two manifest files or versions"
    usage: "Usage: diff [OPTIONS] <BASE_FILE> <CANDIDATE_FILE>"
    hidden: false
    arguments:
      - name: "base_file"
        about: "Base (left) manifest file"
        required: true
      - name: "candidate_file"
        about: "Candidate (right) manifest file"
        required: true
    options:
      - name: "format"
        about: "Output format: json, yaml"
        default: "yaml"
    flags:
      - name: "help"
        about: "Print help"
  - name: "doctor"
    full_path: "doctor"
    about: "Run diagnostics and repair"
    usage: "Usage: doctor [OPTIONS]"
    hidden: false
    flags:
      - name: "repair"
        about: "Auto-repair detected issues"
      - name: "help"
        about: "Print help"
  - name: "head-hash"
    full_path: "head-hash"
    about: "Show the current HEAD commit hash"
    usage: "Usage: head-hash <REPOSITORY>"
    hidden: false
    arguments:
      - name: "repository"
        about: "Repository name"
        required: true
    flags:
      - name: "help"
        about: "Print help"
  - name: "history"
    full_path: "history"
    about: "View commit history for an entity"
    usage: "Usage: history [OPTIONS] <URI>"
    hidden: false
    arguments:
      - name: "uri"
        about: "NAP URI"
        required: true
    options:
      - name: "limit"
        about: "Maximum number of commits to show"
        default: "20"
    flags:
      - name: "help"
        about: "Print help"
  - name: "init"
    full_path: "init"
    about: "Initialize a repository repository and/or configure the backend provider"
    long_about: |
      Initialize a repository repository and/or configure the backend provider.

      When a repository name is provided, creates the repository structure (directories, config, repository manifest, initial commit). When --provider is given (or no provider is configured), sets up the backend provider. Both can be combined:

      nap init toystory                     # create repository nap init toystory --provider local    # create repository + configure provider nap init --provider local             # configure provider only
    usage: "Usage: init [OPTIONS] [REPOSITORY]"
    hidden: false
    arguments:
      - name: "repository"
        about: "Repository name. If provided, initializes a new repository repository"
        required: false
    options:
      - name: "provider"
        about: "Provider type: local, portals-cloud, or remote"
      - name: "remote_url"
        about: "Remote URL (required for remote provider)"
      - name: "workspace_id"
        about: "Workspace ID (for remote provider)"
      - name: "remote"
        about: "Remote URL to add as origin after init"
    flags:
      - name: "reset"
        about: "Reset the provider configuration file"
      - name: "help"
        about: "Print help (see more with '--help')"
  - name: "install"
    full_path: "install"
    about: "Install required dependencies"
    usage: "Usage: install <TARGET>"
    hidden: false
    arguments:
      - name: "target"
        about: "Target to install (e.g., \"lore\" or \"mcp\")"
        required: true
    flags:
      - name: "help"
        about: "Print help"
  - name: "list"
    full_path: "list"
    about: "List repositories or entities within a repository"
    usage: "Usage: list [OPTIONS] [REPOSITORY]"
    hidden: false
    arguments:
      - name: "repository"
        about: "Repository name. Omit to list all repositories"
        required: false
    options:
      - name: "entity_type"
        about: "Entity type to list (if repository is specified)"
    flags:
      - name: "help"
        about: "Print help"
  - name: "merge"
    full_path: "merge"
    about: "Three-way merge of JSON/YAML values"
    usage: "Usage: merge [OPTIONS] <BASE> <CURRENT> <PROPOSED>"
    hidden: false
    arguments:
      - name: "base"
        about: "Base (common ancestor) file"
        required: true
      - name: "current"
        about: "Current (ours) file"
        required: true
      - name: "proposed"
        about: "Proposed (theirs) file"
        required: true
    options:
      - name: "format"
        about: "Output format: json, yaml"
        default: "yaml"
    flags:
      - name: "help"
        about: "Print help"
  - name: "presign"
    full_path: "presign"
    about: "Create a time-limited public URL for a committed representation"
    long_about: |
      Create a time-limited public URL for a committed representation.

      Pass the entity ID first and the representation name second:

      ```bash
      nap presign 25th-chapter/character/nathan-gunn item
      ```

      - `25th-chapter/character/nathan-gunn` identifies the repository, entity type,
        and entity ID. The `nap://` prefix is optional.
      - `item` is the exact key under the entity manifest's `representations` map.
        It is not a file path or the entity's display name.

      The equivalent fully qualified command is:

      ```bash
      nap presign nap://25th-chapter/character/nathan-gunn item
      ```

      ### How the representation is located

      NAP reads the entity manifest at the selected revision and looks up
      `representations.item`. For example:

      ```yaml
      representations:
        item:
          hash: blake3:<content hash>
          format: jpg
          uri: item.jpg
      ```

      Representation URIs are relative to the entity's asset directory, matching
      `nap add`. For this entity, `uri: item.jpg` resolves to
      `character/nathan-gunn/item.jpg` within the repository. Keep `uri: item.jpg`;
      there is no need to put the entity ID into the representation URI.

      ### Revision and lifetime

      ```bash
      nap presign 25th-chapter/character/nathan-gunn item \
        --branch main \
        --ttl-seconds 900
      ```

      Use either `--branch` or `--commit`, never both. When neither is supplied, NAP
      uses the repository's configured default branch, falling back to the global
      default branch. Branches are pinned to a commit before NAP reads the manifest
      and content address. Lore applies its configured lifetime bounds and defaults
      when `--ttl-seconds` is omitted.

      The manifest and representation file must be committed at the selected
      revision, and the content must have been pushed to the Lore server.
      External URLs, linked repositories, absolute paths, path traversal, URI
      fragments, and unversioned working-tree files are not supported.

      ### Output

      In a terminal, the command prints the URL, expiration, and pinned revision.
      When piped or redirected, it emits JSON with `url`, `expires_at`, `revision`,
      `repository_id`, `address`, `representation`, and `format`.

      The returned URL is a bearer capability: anyone who has it can download the
      immutable bytes until it expires. Do not place it in logs, analytics, exception
      messages, source control, or long-lived storage.

      ### Automatic configuration

      NAP records the Lore HTTP origin in `provider.toml` during backend setup and
      backfills older provider configurations automatically. Local Lore uses
      `http://127.0.0.1:41339`; standard remote Lore uses the same host on port 41339;
      TLS deployments behind port 443 use the same HTTPS origin. Portals Cloud uses
      `https://lore.portals.works`. The normal command needs no additional flags:

      ```bash
      nap presign 25th-chapter/character/nathan-gunn item
      ```

      Authenticated requests reuse the active `nap auth login` / Lore identity.
      Only unexpired repository-scoped tokens authorized for the HTTP recipient are
      used. Automatic credential reuse requires HTTPS for remote servers; loopback
      HTTP is supported for development. No separate HTTP token setup is needed.

      Operators with custom proxy layouts can set `http_url` in `provider.toml`.
      Explicit `--http-url` or `NAP_LORE_HTTP_URL` overrides take precedence.
      Bearer-token environment overrides remain available for automation.

      ### Server setup and signing-key security

      New NAP-managed local installations create a unique 32-byte signing key in
      owner-only server configuration and bind to loopback. Existing managed configs
      receive a missing key without replacing existing keys or other settings.
      Restart an already running server after its configuration changes.

      Standalone development Lore provisions a persistent owner-only `presign.key`
      in its configuration directory when no signing key is supplied. Persist this
      directory across restarts. Never copy that key into client configuration.

      Only Lore uses the key, to sign and validate download capabilities. It is
      independent of login tokens, JWT signing keys, and API-key peppers; NAP clients
      never need it. Keep the key stable across restarts and private to the server.
      Server logs omit signing keys and signed query tokens. Signed responses prevent
      caching and referrer leakage. Development URLs require network access to the host.

      Production / Portals Cloud presign is WIP. NAP derives the Cloud HTTP origin,
      but deployment still needs a dedicated shared signing key, scoped HTTPS routes,
      and query-token-safe logging. Without a supplied production key, presign stays
      disabled. These are operator concerns, not end-user flags or secrets.

      ### SDK methods

      All three methods take the entity ID and representation name as their first
      two arguments, using the same lookup as the CLI:

      - Rust: `Resolver::presign_representation(entity_id, representation, &options)`
        is asynchronous.
      - Python: `presign_representation(entity_id, representation, **options)` is
        synchronous.
      - TypeScript: `presignRepresentation(entityId, representation, options)` is
        asynchronous.

      Python:

      ```python
      from nap_sdk import presign_representation

      result = presign_representation(
          "25th-chapter/character/nathan-gunn", "item", branch="main", ttl_seconds=900
      )
      ```

      TypeScript:

      ```typescript
      import { presignRepresentation } from "@portalshq/nap-sdk";

      const result = await presignRepresentation(
        "25th-chapter/character/nathan-gunn",
        "item",
        { branch: "main", ttlSeconds: 900 },
      );
      ```

      The SDKs return the same fields as the CLI JSON output.
    usage: "Usage: presign [OPTIONS] <ENTITY_ID> <REPRESENTATION>"
    hidden: false
    arguments:
      - name: "uri"
        about: "Entity ID, e.g. 25th-chapter/character/nathan-gunn. The nap:// prefix is optional; fragments are not supported"
        required: true
      - name: "representation"
        about: "Representation name (manifest key), e.g. item. Its URI is relative to the entity's asset directory"
        required: true
    options:
      - name: "branch"
        about: "Resolve at a specific branch"
      - name: "commit"
        about: "Resolve at a specific commit hash"
      - name: "ttl_seconds"
        about: "Requested lifetime in seconds; Lore enforces its configured bounds"
      - name: "http_url"
        about: "Explicit Lore HTTP origin, such as http://127.0.0.1:41339"
      - name: "token_env"
        about: "Environment variable containing a repository-scoped bearer token"
    flags:
      - name: "help"
        about: "Print help (see more with '--help')"
  - name: "publish"
    full_path: "publish"
    about: "Publish changes to remote"
    usage: "Usage: publish <REPOSITORY>"
    hidden: false
    arguments:
      - name: "repository"
        about: "Repository name"
        required: true
    flags:
      - name: "help"
        about: "Print help"
  - name: "pull"
    full_path: "pull"
    about: "Clone or pull a repository from a remote"
    long_about: |
      Clone or pull a repository from a remote.

      If the argument is a URL, the repo is cloned (name is read from the repo's own config).  If it's a repository name, the repo must already exist locally and will be updated via pull.
    usage: "Usage: pull <URL_OR_NAME>"
    hidden: false
    arguments:
      - name: "url_or_name"
        about: "URL (clone) or repository name (pull existing)"
        required: true
    flags:
      - name: "help"
        about: "Print help (see more with '--help')"
  - name: "push"
    full_path: "push"
    about: "Push the current branch to its configured upstream remote"
    usage: "Usage: push [OPTIONS] <REPOSITORY>"
    hidden: false
    arguments:
      - name: "repository"
        about: "Repository name"
        required: true
    options:
      - name: "remote"
        about: "Remote name (default: tracking branch's remote, or \"origin\")"
        default: "origin"
      - name: "branch"
        about: "Branch to push (default: current branch)"
    flags:
      - name: "help"
        about: "Print help"
  - name: "query"
    full_path: "query"
    about: "Query a subtree from a manifest"
    usage: "Usage: query [OPTIONS] <URI> <PATH>"
    hidden: false
    arguments:
      - name: "uri"
        about: "NAP URI"
        required: true
      - name: "path"
        about: "Dot-notation path. e.g., \"appearances.audienceVotes\""
        required: true
    options:
      - name: "format"
        about: "Output format: yaml, json"
        default: "json"
    flags:
      - name: "help"
        about: "Print help"
  - name: "remote"
    full_path: "remote"
    about: "Manage remotes on a repository"
    usage: "Usage: remote <COMMAND>"
    hidden: false
    flags:
      - name: "help"
        about: "Print help"
    subcommands:
      - name: "add"
        full_path: "remote add"
        about: "Add a remote to a repository repository"
        usage: "Usage: add <REPOSITORY> <NAME> <URL>"
        hidden: false
        arguments:
          - name: "repository"
            about: "Repository name"
            required: true
          - name: "name"
            about: "Remote name (e.g., \"origin\")"
            required: true
          - name: "url"
            about: "Remote URL"
            required: true
        flags:
          - name: "help"
            about: "Print help"
      - name: "ls"
        full_path: "remote ls"
        about: "List remotes on a repository repository"
        usage: "Usage: ls <REPOSITORY>"
        hidden: false
        arguments:
          - name: "repository"
            about: "Repository name"
            required: true
        flags:
          - name: "help"
            about: "Print help"
      - name: "rm"
        full_path: "remote rm"
        about: "Remove a remote from a repository repository"
        usage: "Usage: rm <REPOSITORY> <NAME>"
        hidden: false
        arguments:
          - name: "repository"
            about: "Repository name"
            required: true
          - name: "name"
            about: "Remote name to remove"
            required: true
        flags:
          - name: "help"
            about: "Print help"
  - name: "resolve"
    full_path: "resolve"
    about: "Resolve a NAP URI to its manifest or a subtree"
    long_about: |
      Resolve a NAP URI to its manifest or a subtree.

      Fragment queries are supported via the URI: nap resolve nap://toystory/character/woody#references.appears_in
    usage: "Usage: resolve [OPTIONS] <URI>"
    hidden: false
    arguments:
      - name: "uri"
        about: "NAP URI. e.g., \"nap://toystory/character/woody\""
        required: true
    options:
      - name: "branch"
        about: "Resolve at a specific branch"
      - name: "commit"
        about: "Resolve at a specific commit hash"
      - name: "format"
        about: "Output format: yaml, json"
        default: "yaml"
    flags:
      - name: "provenance"
        about: "Include condensed per-file provenance for the manifest and direct representations"
      - name: "include_blobs"
        about: "Hydrate known readable provenance artifacts such as prompts and run records"
      - name: "help"
        about: "Print help (see more with '--help')"
  - name: "revert"
    full_path: "revert"
    about: "Revert a commit by hash (undoes all changes in that commit)"
    usage: "Usage: revert [OPTIONS] --commit <COMMIT> <REPOSITORY>"
    hidden: false
    arguments:
      - name: "repository"
        about: "Repository name"
        required: true
    options:
      - name: "commit"
        about: "Commit hash to revert"
      - name: "author"
        about: "Author identifier"
        default: "nap-cli"
    flags:
      - name: "help"
        about: "Print help"
  - name: "schema"
    full_path: "schema"
    about: "Print a JSON Schema for manifest or commit types"
    usage: "Usage: schema [OPTIONS] <NAME>"
    hidden: false
    arguments:
      - name: "name"
        about: "Schema name: 'manifest' or 'commit'"
        required: true
    options:
      - name: "format"
        about: "Output format: json, yaml"
        default: "json"
    flags:
      - name: "help"
        about: "Print help"
  - name: "set"
    full_path: "set"
    about: "Set a property on an entity manifest"
    usage: "Usage: set [OPTIONS] <URI> <KEY> <VALUE>"
    hidden: false
    arguments:
      - name: "uri"
        about: "NAP URI"
        required: true
      - name: "key"
        about: "Property key (dot-notation)"
        required: true
      - name: "value"
        about: "Property value"
        required: true
    options:
      - name: "message"
        about: "Commit message"
        default: "set property"
      - name: "author"
        about: "Author identifier"
        default: "nap-cli"
    flags:
      - name: "help"
        about: "Print help"
  - name: "sign"
    full_path: "sign"
    about: "Sign a manifest (stub for v0)"
    usage: "Usage: sign <URI>"
    hidden: false
    arguments:
      - name: "uri"
        about: "NAP URI"
        required: true
    flags:
      - name: "help"
        about: "Print help"
  - name: "status"
    full_path: "status"
    about: "Show system status"
    usage: "Usage: status"
    hidden: false
    flags:
      - name: "help"
        about: "Print help"
  - name: "switch"
    full_path: "switch"
    about: "Switch to a branch"
    usage: "Usage: switch <REPOSITORY> <NAME>"
    hidden: false
    arguments:
      - name: "repository"
        about: "Repository name"
        required: true
      - name: "name"
        about: "Branch name to switch to"
        required: true
    flags:
      - name: "help"
        about: "Print help"
  - name: "sync"
    full_path: "sync"
    about: "Sync with remote"
    usage: "Usage: sync <REPOSITORY>"
    hidden: false
    arguments:
      - name: "repository"
        about: "Repository name"
        required: true
    flags:
      - name: "help"
        about: "Print help"
  - name: "validate"
    full_path: "validate"
    about: "Validate a manifest against the NAP schema"
    usage: "Usage: validate [OPTIONS] [URI]"
    hidden: false
    arguments:
      - name: "uri"
        about: "NAP URI of the entity to validate"
        required: false
    options:
      - name: "file"
        about: "Path to a manifest YAML file to validate"
    flags:
      - name: "help"
        about: "Print help"
  - name: "verify"
    full_path: "verify"
    about: "Verify a manifest signature (stub for v0)"
    usage: "Usage: verify <URI>"
    hidden: false
    arguments:
      - name: "uri"
        about: "NAP URI"
        required: true
    flags:
      - name: "help"
        about: "Print help"
